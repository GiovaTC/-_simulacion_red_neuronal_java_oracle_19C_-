# -_simulacion_red_neuronal_java_oracle_19C_- :.
🧠 Simulación de Red Neuronal en Java + Oracle 19c:

<img width="1024" height="1024" alt="image" src="https://github.com/user-attachments/assets/eabb85ae-c478-4b04-83ed-434751f1b0cb" />  

```
A continuación tienes una solución completa, estructurada y ejecutable en Java (IntelliJ) que simula una red neuronal simple (tipo perceptrón multicapa básico) para calcular:

✅ Suma de 5 notas
✅ Promedio

📦 Persistencia en Oracle 19c mediante JDBC + Procedimiento Almacenado (PL/SQL)

🧩 1. Modelo Conceptual (Red Neuronal Simplificada):

Simulación de una red neuronal básica:

Entradas (5 notas)
   ↓
Capa oculta (ponderación simple)
   ↓
Salida 1: Suma
Salida 2: Promedio

👉 No es entrenamiento real con backpropagation, pero modela el flujo neuronal:

Pesos = 1
Función de activación = identidad .

🗄️ 2. Script Oracle 19c:
📌 Tabla
CREATE TABLE NOTAS_ALUMNOS (
    ID              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_ALUMNO   VARCHAR2(100),
    NOTA1           NUMBER(5,2),
    NOTA2           NUMBER(5,2),
    NOTA3           NUMBER(5,2),
    NOTA4           NUMBER(5,2),
    NOTA5           NUMBER(5,2),
    SUMA            NUMBER(6,2),
    PROMEDIO        NUMBER(5,2),
    FECHA_REGISTRO  DATE DEFAULT SYSDATE
);

📌 Procedimiento Almacenado
CREATE OR REPLACE PROCEDURE INSERTAR_NOTAS (
    P_NOMBRE   IN VARCHAR2,
    P_N1       IN NUMBER,
    P_N2       IN NUMBER,
    P_N3       IN NUMBER,
    P_N4       IN NUMBER,
    P_N5       IN NUMBER,
    P_SUMA     IN NUMBER,
    P_PROM     IN NUMBER
)
AS
BEGIN
    INSERT INTO NOTAS_ALUMNOS (
        NOMBRE_ALUMNO, NOTA1, NOTA2, NOTA3, NOTA4, NOTA5, SUMA, PROMEDIO
    ) VALUES (
        P_NOMBRE, P_N1, P_N2, P_N3, P_N4, P_N5, P_SUMA, P_PROM
    );

    COMMIT;
END;
/

☕ 3. Programa Java (IntelliJ):
📌 Dependencia JDBC (Maven)
<dependency>
    <groupId>com.oracle.database.jdbc</groupId>
    <artifactId>ojdbc8</artifactId>
    <version>19.3.0.0</version>
</dependency>

📌 Clase RedNeuronal (Lógica)
public class RedNeuronal {

    // Simulación de neurona (suma ponderada)
    public static double calcularSuma(double[] entradas) {
        double suma = 0;
        for (double valor : entradas) {
            suma += valor; // peso = 1
        }
        return suma;
    }

    public static double calcularPromedio(double suma) {
        return suma / 5.0;
    }
}

📌 Clase ConexionOracle
import java.sql.Connection;
import java.sql.DriverManager;

public class ConexionOracle {

    public static Connection getConnection() throws Exception {
        String url = "jdbc:oracle:thin:@localhost:1521:XE";
        String user = "SYSTEM";
        String pass = "1234";

        return DriverManager.getConnection(url, user, pass);
    }
}

📌 Clase Main
import java.sql.CallableStatement;
import java.sql.Connection;
import java.util.Scanner;

public class Main {

    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);

        try (Connection conn = ConexionOracle.getConnection()) {

            for (int i = 1; i <= 4; i++) {

                System.out.println("\nAlumno #" + i);
                System.out.print("Nombre: ");
                String nombre = sc.nextLine();

                double[] notas = new double[5];

                for (int j = 0; j < 5; j++) {
                    System.out.print("Nota " + (j + 1) + ": ");
                    notas[j] = sc.nextDouble();
                }
                sc.nextLine(); // limpiar buffer

                // 🔹 "Red neuronal"
                double suma = RedNeuronal.calcularSuma(notas);
                double promedio = RedNeuronal.calcularPromedio(suma);

                System.out.println("Suma: " + suma);
                System.out.println("Promedio: " + promedio);

                // 🔹 Llamada a procedimiento almacenado
                CallableStatement cs = conn.prepareCall("{call INSERTAR_NOTAS(?,?,?,?,?,?,?,?)}");

                cs.setString(1, nombre);
                cs.setDouble(2, notas[0]);
                cs.setDouble(3, notas[1]);
                cs.setDouble(4, notas[2]);
                cs.setDouble(5, notas[3]);
                cs.setDouble(6, notas[4]);
                cs.setDouble(7, suma);
                cs.setDouble(8, promedio);

                cs.execute();

                System.out.println("✔ Registro guardado en Oracle");

                cs.close();
            }

        } catch (Exception e) {
            e.printStackTrace();
        }

        sc.close();
    }
}

📊 4. Ejemplo de Ejecucion:
Alumno #1
Nombre: Juan
Nota 1: 4.0
Nota 2: 3.5
Nota 3: 4.2
Nota 4: 3.8
Nota 5: 4.5

Suma: 20.0
Promedio: 4.0
✔ Registro guardado en Oracle .

🧠 5. Explicación Tecnica:
🔹 ¿Por qué se considera una “red neuronal”?
Entradas → notas
Neuronas → funciones de suma
Salidas → suma y promedio
🔹 Simplificación aplicada
Pesos = 1
Sin entrenamiento
Activación lineal

👉 Es una abstracción didáctica de una red neuronal.

🚀 6. Posibles Mejoras (Nivel Pro)

Para escalar el proyecto:

🔸 Implementar función de activación sigmoide
🔸 Incorporar backpropagation
🔸 Guardar historial en otra tabla
🔸 Crear interfaz gráfica (Swing / JavaFX)
🔸 Aplicar patrón DAO + MVC .

✅ Resultado Final:
✔ Simulación de red neuronal
✔ Cálculo automático de suma y promedio
✔ Persistencia en Oracle 19c
✔ Uso de procedimiento almacenado
✔ Código listo para IntelliJ : . / .
